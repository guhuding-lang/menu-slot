# 嘎巴47周报 / 月报 Design QA（v46）

## 视觉真值与实现证据

- 周报正式背景：`/workspace/scratch/62206c759608/upload/01-1000205678.png`（1024 × 1536）
- 月报正式背景：`/workspace/scratch/62206c759608/upload/02-1000205677.png`（1024 × 1536）
- 周报内容参考：`/workspace/scratch/62206c759608/upload/03-1000205507.png`（1024 × 1536）
- 月报内容参考：`/workspace/scratch/62206c759608/upload/04-1000205511.png`（1024 × 1536）
- 用户指出问题的周报：`/workspace/scratch/62206c759608/upload/01-1000205696.png`（1024 × 1536）
- v46 周报整页浏览器截图：`/workspace/scratch/gaba47-weekly-v46-fullfix.jpg`
- v46 月报整页浏览器截图：`/workspace/scratch/gaba47-monthly-v46-fullfix.jpg`

## 状态与尺寸

- 本地页面：`http://terminal.local:4173/gaba47/`
- 周报状态：`?preview=1&report=week&qa=v46b`
- 月报状态：`?preview=1&report=month&qa=v46b`
- 报告 Canvas：1080 × 1620，2:3。
- 正式脚本：`app.js?v=46`；离线缓存：`gaba47-v54`。

## 周报完整画面复核

- 直接使用用户指定的周报背景，保留顶部四项指标、地图 TOP 3、冒险奖励、其他训练成员和底部标语。
- 第一名使用金色真实徽章，第二名和第三名分别使用蓝色、铜色徽章；三个名次均完整进入卡片。
- TOP 3 丝带只绘制一次标题，不再与背景星星重复叠加。
- 长昵称不再先做生硬字符截断，而是在卡片宽度内自动缩小；其他成员昵称最多两行。
- 冒险奖励图标、奖励名和归属值整体上移，五组内容均位于紫色框内。
- 其他成员时长与次数上移，空状态位于第一排留白区中央，不压两排卡片分隔线。
- 底部“每一次打卡，都是成长的足迹！”完整居中显示在卷轴内。

## 月报完整画面复核

- 直接使用用户指定的月报背景，保留五项指标、本月之星、星座参与环、TOP 3、趋势、连续天数分布、日历与底部标语。
- 十二星座节点全部显示星座名称、训练时长和打卡次数；节点尺寸、文字与冠军光晕均扩大。
- 本月 TOP 3 的昵称、时长和次数整体上移，避免贴近卡片底边。
- 趋势图补充月初、月中和“峰值 Xh · M/D”标记，峰值信息可读。
- 连续天数分布和日历字号、单元格高度提升，数据均位于原生框内。
- 底部标语完整显示，不与卷轴边缘重叠。

## 动态模块与控制台

- `homeFeedItems()` 从 `.slice(0, 20)` 改为 `.slice(0, 50)`。
- 本地预览实际显示“32 条最近动态”，证明页面不再在第 20 条截断；数据超过 50 条时仅保留最近 50 条。
- 周报和月报各检测到 1 张生成报告图。
- 周报、月报浏览器控制台均无项目自身错误。
- `git diff --check`、`node --check gaba47/app.js`、`node --check gaba47/sw.js` 均通过。

## 允许的 P3 差异

- 参考图左上品牌块、右上方案标记和标题装扮猫不在用户指定的正式背景中，因此不额外覆盖标题区。
- 榜单猫咪使用成员实际选择的星座猫，日期和数值使用真实训练数据，不复刻参考图示例数据。

**final result: passed**
