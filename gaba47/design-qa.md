# 嘎巴47周报 / 月报 Design QA

## 验收范围

- 周报参考：`/workspace/scratch/62206c759608/upload/03-1000205507.png`
- 月报参考：`/workspace/scratch/62206c759608/upload/04-1000205511.png`
- 正式页面：`https://guhuding-lang.github.io/menu-slot/gaba47/`
- 验收状态：GitHub Pages 正式环境，`app.js?v=40`
- 海报尺寸：周报、月报均为 1080 × 1620（2:3）
- 数据状态：使用项目现有统计函数生成，预览数据只用于覆盖视觉状态

## 参考图落实情况

- 周报恢复“冒险地图”结构：足迹路线贯穿前三名角色卡，第一名居中放大，第二、三名错落排布。
- 周报保留累计时长、活跃人数、打卡次数、人均时长、TOP 3、冒险奖励、其他成员和页脚。
- 冒险奖励改为五枚独立像素徽章，并由真实排名、打卡次数和总时长驱动。
- 月报以“本月之星 + 十二星座参与环”为视觉核心，冠军星座使用金色描边和外发光。
- 月报保留总训练时长、活跃人数、总打卡数、最长连续、人均时长、TOP 3、趋势、连续天数分布、日历、其他成员和页脚。
- 按需求不显示参考图左上角品牌块和右上角“方案”标记。

## 数据与适配

- 所有时长、次数、活跃人数、连续天数、趋势和日历均由真实打卡记录计算，没有写死参考图数字。
- 榜单使用成员当前选择的星座猫形象。
- 十二星座参与环展示每个星座的总训练时长；无训练的星座仍保留在环上。
- 其他成员固定四列，最多展示八人，超过八人时显示剩余人数提示。
- 无其他训练成员时保留模块并展示明确空状态，不留下大面积无意义空白。

## 正式环境检查

| 检查项 | 结果 | 证据 |
|---|---|---|
| 页面身份 | 通过 | 标题为“嘎巴47”，正式脚本为 `app.js?v=40` |
| 页面非空 | 通过 | 动态、报告入口、底部导航和报告弹层正常渲染 |
| 框架错误层 | 通过 | 未出现 Vite、Webpack 或运行时错误覆盖层 |
| 控制台 | 通过 | 无应用代码错误；仅有云端浏览器扩展自身元数据日志 |
| 周报视觉 | 通过 | 地图、足迹、三张角色卡、五枚奖励和成员模块均可见 |
| 月报视觉 | 通过 | 本月之星、十二星座环、冠军光晕、TOP 3、趋势、分布和日历均可见 |
| 交互 | 通过 | 关闭月报后弹层消失；点击最近一期后周报弹层重新打开 |
| 导出入口 | 通过 | “保存图片”按钮保留，高清海报图已生成 |

## 允许的低优先级差异

- 参考图内的猫咪动作、奖励图标为概念稿专属素材；正式版复用项目现有星座猫资产和程序化徽章，避免角色形象不一致。
- 预览周期只有三位活跃成员，因此其他成员显示空状态；正式数据增加后会自动填充。

**final result: passed**
